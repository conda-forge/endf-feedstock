{% set name = "endf" %}
{% set version = "0.1.9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/endf-{{ version }}.tar.gz
  sha256: 37463da089fe051466ef7cef14fa6de8061a8e6bd6ea8b58714d91d7239963f9

build:
  skip: true  # [py<38]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 1

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - pybind11                               # [build_platform != target_platform]
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - python
    - setuptools
    - pybind11
    - setuptools-scm
    - pip
  run:
    - python
    - numpy
    - uncertainties

test:
  imports:
    - endf
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/paulromano/endf-python
  summary: Python interface to ENDF-6 files
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - paulromano
    - shimwell
